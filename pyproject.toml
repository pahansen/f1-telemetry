[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "f1-telemetry"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "typer>=0.21.1",
]

[project.scripts]
f1-telemetry = "f1_telemetry.main:app"

[dependency-groups]
dev = [
    "black>=26.1.0",
]
